[project]
name = "py-lab"
version = "0.1.0"
description = "Data Structures and Algorithms in python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
